import 'package:flutter/material.dart';
import '../../../widgets/floating_button.dart';

class UpdatesScreenFloatingButton extends StatelessWidget {
  const UpdatesScreenFloatingButton({super.key});

  @override
  Widget build(BuildContext context) {
    return AppFloatingActionButton(icon: Icons.camera_alt, onp: () {});
  }
}
